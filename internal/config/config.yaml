app:
  name: "gRPCCalculator"
  version: "1.0.0"

server:
  host: "0.0.0.0"
  port: "8080"
  read_timeout: "10s"
  write_timeout: "10s"

database:
  dbPath: "./storage/calculator.db"
  migrationsPath: "./migrations"

auth:
  jwt_secret: "yandex-go"
  token_expires: "24h"
